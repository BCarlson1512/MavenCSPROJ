
public enum Types {
	/**
	 *  for added types, This is an enum file which just allows for easy access
	 *  
	 *  In the last project I did, we used this for game states, but we might just avoid using an enum all together
	 */

	NORMAL, GRASS, FIRE, WATER;
}
